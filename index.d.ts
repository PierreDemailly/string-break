/**
 * Add a maximum column length to a string without breaking words (if possible).
 * @param {string} [string]
 * @param {number} [maxColLength=80] Default is 80.
 */
export function stringBreak(string: string, maxColLength = 80): string;
